name: zstd_dart
version: 1.0.0
description: Zstd compression/decompression for Dart using native build hooks.

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  ffi: ^2.0.0
  hooks: ^1.0.2
  code_assets: ^1.0.0
  native_toolchain_c: ^0.17.6

dev_dependencies:
  test: ^1.16.0
