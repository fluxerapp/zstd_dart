name: zstd_dart
version: 1.1.0
description: Zstandard compression for Dart with one-shot and stateful streaming APIs, powered by native build hooks.
repository: https://github.com/fluxerapp/zstd_dart
issue_tracker: https://github.com/fluxerapp/zstd_dart/issues
topics:
  - compression
  - zstd
  - ffi
  - native-assets
platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  ffi: ^2.0.0
  hooks: ^1.0.2
  code_assets: ^1.0.0
  native_toolchain_c: ^0.17.6

dev_dependencies:
  test: ^1.16.0
